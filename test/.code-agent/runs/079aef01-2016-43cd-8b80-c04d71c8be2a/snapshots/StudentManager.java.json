{"path": "StudentManager.java", "content": "import java.util.ArrayList;\nimport java.util.List;\n\n/**\n * Simple Student Manager with incomplete features.\n * Used for testing NaturalCC code completion and repair.\n */\npublic class StudentManager {\n    private List<Student> students;\n\n    /**\n     * Constructs an empty StudentManager.\n     */\n    public StudentManager() {\n        this.students = new ArrayList<>();\n    }\n\n    /**\n     * Adds a student with the given name and age.\n     *\n     * @param name the student's name (must not be null)\n     * @param age  the student's age (must be positive)\n     * @return true if the student was added successfully, false otherwise\n     */\n    // TODO: complete this method - should add a student and return true if successful\n    public boolean addStudent(String name, int age) {\n        if (name == null || age <= 0) {\n            return false;\n        }\n        students.add(new Student(name, age));\n        return true;\n    }\n\n    /**\n     * Finds a student by name.\n     *\n     * @param name the name to search for\n     * @return the Student with the given name, or null if not found\n     */\n    // TODO: complete this method - should find a student by name\n    public Student findStudent(String name) {\n        for (Student s : students) {\n            if (s.getName().equals(name)) {\n                return s;\n            }\n        }\n        return null;\n    }\n\n    /**\n     * Calculates the average age of all students.\n     *\n     * @return the average age, or 0.0 if there are no students\n     */\n    // BUG: should check for empty list before calculating average\n    public double calculateAverageAge() {\n        if (students.isEmpty()) {\n            return 0.0;\n        }\n        int total = 0;\n        for (Student s : students) {\n            total += s.getAge();\n        }\n        return (double) total / students.size();\n    }\n\n    /**\n     * Removes the first occurrence of a student with the given name.\n     *\n     * @param name the name of the student to remove\n     */\n    // BUG: does not check for duplicate names\n    public void removeStudent(String name) {\n        for (int i = 0; i < students.size(); i++) {\n            if (students.get(i).getName().equals(name)) {\n                students.remove(i);\n                break;\n            }\n        }\n    }\n\n    /**\n     * Returns the number of students currently managed.\n     *\n     * @return the student count\n     */\n    public int getStudentCount() {\n        return students.size();\n    }\n\n    /**\n     * Represents a student with a name and age.\n     */\n    public static class Student {\n        private String name;\n        private int age;\n\n        /**\n         * Constructs a Student with the given name and age.\n         *\n         * @param name the student's name\n         * @param age  the student's age\n         */\n        public Student(String name, int age) {\n            this.name = name;\n            this.age = age;\n        }\n\n        /**\n         * Returns the student's name.\n         *\n         * @return the name\n         */\n        public String getName() { return name; }\n\n        /**\n         * Returns the student's age.\n         *\n         * @return the age\n         */\n        public int getAge() { return age; }\n\n        /**\n         * Sets the student's name.\n         *\n         * @param name the new name\n         */\n        public void setName(String name) { this.name = name; }\n\n        /**\n         * Sets the student's age.\n         *\n         * @param age the new age\n         */\n        public void setAge(int age) { this.age = age; }\n\n        @Override\n        public String toString() {\n            return \"Student{name='\" + name + \"', age=\" + age + \"}\";\n        }\n    }\n\n    /**\n     * Main method for demonstration.\n     *\n     * @param args command-line arguments (not used)\n     */\n    public static void main(String[] args) {\n        StudentManager manager = new StudentManager();\n        manager.addStudent(\"Alice\", 20);\n        manager.addStudent(\"Bob\", 22);\n        System.out.println(\"Total students: \" + manager.getStudentCount());\n        System.out.println(\"Average age: \" + manager.calculateAverageAge());\n    }\n}\n"}